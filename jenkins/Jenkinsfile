#!groovy

node {
    docker.image('sbtscala/scala-sbt') {
        stage('version') {
            sh 'echo Test Gatling'
        }

        stage('Running') {
            sh 'mvn gatling:test'
        }
    }
}