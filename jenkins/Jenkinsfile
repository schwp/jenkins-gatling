#!groovy

node {
    docker.image('denvazh/gatling:latest') {
        stage('version') {
            sh 'echo Test Gatling'
        }

        stage('Running') {
            sh 'mvn gatling:test'
        }
    }
}